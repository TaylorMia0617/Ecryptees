{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "main-capability",
    "description": "Minimal permissions for the local Ecryptees desktop window",
    "windows": ["main"],
    "permissions": [
        "core:default",
        "dialog:allow-open"
    ]
}
